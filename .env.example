# Meta WhatsApp Cloud API — Júlia (Suporte Keth)
META_PHONE_NUMBER_ID=1178750298660315
META_ACCESS_TOKEN=your_system_user_token
# WhatsApp Business Account id
META_WABA_ID=1521012845568398

# Meta Webhook (POST /webhooks/meta)
META_APP_SECRET=your_app_secret_from_meta_developers
META_VERIFY_TOKEN=your_custom_verify_token

# Supabase
SUPABASE_URL=https://tzqjezbsysetkmzxpuet.supabase.co
SUPABASE_KEY=your_service_role_key

# Scheduler poll interval in seconds (default: 30)
SCHEDULER_INTERVAL_SECONDS=30
SCHEDULER_BATCH_SIZE=50

# Retry policy for transient send failures
MESSAGE_MAX_ATTEMPTS=3
MESSAGE_RETRY_BASE_MINUTES=5

# Cost sync (Meta pricing_analytics)
COST_SYNC_INTERVAL_SECONDS=86400
COST_SYNC_LOOKBACK_DAYS=7

# Security — API key for admin/dashboard endpoints
# Generate: python3 -c "import secrets; print(secrets.token_urlsafe(32))"
API_KEY=your_generated_api_key_here

# OpenAI — Júlia agent (GPT-4.1)
OPENAI_API_KEY=sk-your-openai-api-key

# Redis — Júlia agent session memory (Railway provides REDIS_URL automatically)
REDIS_URL=redis://localhost:6379/0

# Chatwoot — forward incoming WhatsApp messages for human support
CHATWOOT_WEBHOOK_URL=https://chatwoot-keth.itbtqc.easypanel.host/webhooks/whatsapp/+5521982320742
